const username = document.getElementById("username");
const password = document.getElementById("password");

const cancelButton = document.getElementById("cancelAccountButton").classList
const saveButton = document.getElementById("submitAccountButton").classList

window.onload = (event) => {




}

function enableButton() {
  if (username.value == "" && password.value == "") {

  }

  if (username.value != "" || password.value != "") {

  }
}

